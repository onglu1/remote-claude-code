# remote-cc 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development 或 superpowers:executing-plans 逐任务实现。步骤用 `- [ ]` 复选框跟踪。

**Goal:** 一个手机优先的网页网关，作为服务器上 Claude Code 会话的「远程窗口」：口令登录、显式登记项目、对会话终端直传、tmux 持久化随时恢复，并提供只读文件浏览与 task/evidence 浏览+轻量管理。

**Architecture:** 单仓 Node 22 + npm workspaces（`packages/shared` / `apps/server` / `apps/web`）。后端 Fastify，会话引擎 = node-pty attach 到独立 `tmux -L rcc` 会话内运行的 `claude`，经 WebSocket 与前端 xterm.js 终端直传。鉴权用单口令 argon2id + 签名 token，暴露沿用 Cloudflare Tunnel。前端 React+Vite，手机优先、简约设计。

**Tech Stack:** TypeScript, Fastify, @fastify/websocket, node-pty, tmux, argon2, zod, React, Vite, xterm.js (+ fit-addon), vitest。

---

## 文件结构

```
remote-cc/
  package.json                      根 workspaces
  tsconfig.base.json
  .env.example
  config/projects.json              项目注册表（显式，零扫描）
  packages/shared/src/
    index.ts                        汇总导出
    schemas.ts                      zod：Project/Conversation/FileEntry/Task/Evidence
    ws.ts                           WS 协议类型
  apps/server/src/
    index.ts                        入口
    app.ts                          组装 Fastify
    config.ts                       env 校验
    lib/
      projects.ts                   注册表读写（无扫描）
      session/tmux.ts               tmux CLI 薄封装（纯命令拼装 + 解析）
      session/ptyBridge.ts          node-pty attach 封装
      session/registry.ts           活动会话注册表
      files.ts                      只读文件浏览 + 路径防护
      taskEvidence.ts               解析 + 写回
    plugins/
      requireAdmin.ts               鉴权
      security.ts                   安全头
      staticSite.ts                 前端静态托管
    routes/
      auth.ts  projects.ts  files.ts  taskEvidence.ts  sessionWs.ts
  apps/web/src/
    main.tsx App.tsx
    lib/api.ts lib/ws.ts lib/router.tsx
    themes/tokens.css
    components/Login.tsx ProjectList.tsx ConversationList.tsx
              Terminal.tsx FileBrowser.tsx TaskEvidenceBoard.tsx
  deploy/  README + compose/cloudflared 示例
```

---

## 里程碑 1：核心闭环

### Task 1: 仓库脚手架与 workspaces

**Files:** Create `package.json`, `tsconfig.base.json`, `.gitignore`, `.nvmrc`, `.env.example`, `packages/shared/package.json`, `apps/server/package.json`, `apps/web/package.json`

- [ ] 建根 `package.json`：`workspaces: ["packages/shared","apps/server","apps/web"]`，`engines.node>=22`，scripts：`build`（shared→web→server）、`dev:server`、`dev:web`、`test`、`typecheck`。
- [ ] 建三个子包 `package.json` 与 `tsconfig`，shared 先编译出 `dist`。
- [ ] `npm install` 成功；`npm run typecheck` 通过（空实现）。
- [ ] Commit：`chore: monorepo scaffold`。

### Task 2: 共享类型（packages/shared）

**Files:** Create `packages/shared/src/{schemas.ts,ws.ts,index.ts}`, Test `schemas.test.ts`

- [ ] 写 zod：`ProjectSchema`（id/name/path/type('dev'|'research')/launchCommand?/notes?/browseRoots?）、`ConversationSchema`、`FileEntrySchema`、`TaskItemSchema`、`EvidenceItemSchema`。
- [ ] 写 WS 协议 union：client `{type:'input',data}` | `{type:'resize',cols,rows}`；server `{type:'data',data}` | `{type:'status',alive}` | `{type:'exit',code}`。
- [ ] 测试：合法/非法 Project 解析（type 限定、缺字段报错）。先红后绿。
- [ ] Commit：`feat(shared): DTO schemas`。

### Task 3: 后端配置与最小 app

**Files:** Create `apps/server/src/{config.ts,app.ts,index.ts}`, Test `config.test.ts`, `app.test.ts`

- [ ] `config.ts`：zod 解析 env（PORT/HOST/ADMIN_PASSWORD(_HASH)/SESSION_SECRET/TOKEN_TTL_HOURS/PUBLIC_ORIGIN/TRUST_CLOUDFLARE/TMUX_SOCKET/PROJECTS_CONFIG），缺 SESSION_SECRET 报错。
- [ ] `app.ts`：`buildApp()` 返回 Fastify 实例，注册 `GET /api/health → {ok:true}`。
- [ ] 测试：`buildApp().inject({url:'/api/health'})` 返回 200。
- [ ] Commit：`feat(server): config + health`。

### Task 4: 鉴权 requireAdmin

**Files:** Create `apps/server/src/plugins/requireAdmin.ts`, `routes/auth.ts`, Test `requireAdmin.test.ts`

- [ ] 启动时 argon2id 哈希 `ADMIN_PASSWORD`（或用 `ADMIN_PASSWORD_HASH`）。
- [ ] `POST /api/auth/unlock {password}`：校验 → 签发 httpOnly cookie token（HMAC-SHA256(`SESSION_SECRET`)，含过期戳）。`POST /api/auth/lock` 清 cookie。`GET /api/auth/state`。
- [ ] `requireAdmin` 装饰器/preHandler：校验 cookie token，失败 401。
- [ ] 测试：错口令 401；对口令拿 token；带 token 过受保护路由；过期 token 401。
- [ ] Commit：`feat(server): single-password auth`。

### Task 5: 项目注册表（无扫描）

**Files:** Create `apps/server/src/lib/projects.ts`, `routes/projects.ts`, `config/projects.json`(空数组), Test `projects.test.ts`

- [ ] `loadProjects()` 读 `PROJECTS_CONFIG` JSON → `ProjectSchema[]`（文件不存在视为 `[]`，绝不扫描目录）。
- [ ] `addProject(p)` 校验 path 为绝对路径且存在、id 唯一 → 原子写（temp+rename）+ `.bak` 备份。`removeProject(id)`、`getProject(id)`。
- [ ] 路由：`GET /api/projects`、`POST /api/projects`、`DELETE /api/projects/:id`（均 requireAdmin）。
- [ ] 测试：add→load 往返；重复 id 拒绝；相对路径拒绝；写回生成 .bak。
- [ ] Commit：`feat(server): project registry`。

### Task 6: tmux 薄封装

**Files:** Create `apps/server/src/lib/session/tmux.ts`, Test `tmux.test.ts`

- [ ] 纯函数拼装命令（注入 `exec` 便于测试）：
  - `sessionName(projectId,convId) => 'rcc-'+projectId+'-'+convId`
  - `newOrAttachArgs(name, cwd, cmd, cols, rows)` → `['-L',socket,'new-session','-A','-s',name,'-c',cwd,'-x',cols,'-y',rows,'--', ...cmd]`
  - `listSessions()` → 解析 `tmux -L rcc list-sessions -F '#{session_name}'`，过滤 `rcc-` 前缀
  - `killSession(name)`、`capturePane(name,lines)` → `capture-pane -p -t name -S -lines`
  - `isAvailable()` → `tmux -V` 探测
- [ ] 测试：参数拼装快照；list 解析（含空输出）；name 规则。
- [ ] Commit：`feat(server): tmux wrapper`。

### Task 7: pty 桥 + 会话注册表

**Files:** Create `apps/server/src/lib/session/{ptyBridge.ts,registry.ts}`, Test `registry.test.ts`

- [ ] `createPtyBridge({name,cwd,cmd,cols,rows})`：`pty.spawn('tmux', newOrAttachArgs(...))`，暴露 `onData(cb)`、`write(d)`、`resize(c,r)`、`dispose()`（dispose 只 kill attach 进程，不 kill tmux）。
- [ ] `SessionRegistry`：`Map<convId,bridge>`；`attach(conv)` 复用或新建 bridge；`detach(convId)`；`reconcile()` 启动时按 `listSessions()` 标记存活。注入 bridge 工厂便于测试。
- [ ] 测试（用假 bridge 工厂）：attach 复用、detach 释放、reconcile 标记。
- [ ] Commit：`feat(server): pty bridge + registry`。

### Task 8: 会话 REST + WS 流

**Files:** Create `apps/server/src/routes/sessionWs.ts`, Modify `app.ts`(注册 @fastify/websocket), Test `sessionWs.test.ts`

- [ ] REST：`GET /api/projects/:id/conversations`（list+alive）、`POST .../conversations {name}`（建会话元数据，存 `config/conversations.json`）、`DELETE .../conversations/:cid`（kill tmux）。
- [ ] WS：`GET /api/projects/:id/conversations/:cid/stream`（requireAdmin via cookie）：attach bridge；`bridge.onData → ws.send {type:'data'}`；收 `{type:'input'}→write`、`{type:'resize'}→resize`；ws close → `registry.detach`。
- [ ] 测试：未授权 WS 拒绝；元数据 CRUD；input/resize 帧路由到假 bridge。
- [ ] Commit：`feat(server): session ws`。

### Task 9: 前端脚手架 + 设计 token + 路由

**Files:** Create `apps/web/{index.html,vite.config.ts}`, `src/{main.tsx,App.tsx,index.css}`, `src/themes/tokens.css`, `src/lib/{api.ts,router.tsx}`

- [ ] Vite + React + TS；`vite.config` 代理 `/api`→后端，build 输出供 server 静态托管。
- [ ] `tokens.css`：系统字体栈、单一强调色、中性表面、间距/圆角变量；无渐变/无毛玻璃。
- [ ] `api.ts`：fetch 封装（带 cookie）。`router.tsx`：登录/项目列表/项目详情/会话 路由。
- [ ] Commit：`feat(web): scaffold + tokens`。

### Task 10: 登录 + 项目列表 + 添加项目

**Files:** Create `apps/web/src/components/{Login.tsx,ProjectList.tsx}`

- [ ] Login：口令输入 → `POST /api/auth/unlock` → 进项目列表。
- [ ] ProjectList：拉 `/api/projects`，列表式卡片（名/类型/路径），点进入；「添加项目」表单（name/path/type/launchCommand）→ POST。
- [ ] Commit：`feat(web): login + projects`。

### Task 11: 终端组件（xterm 直传 + 手机功能键）

**Files:** Create `apps/web/src/components/{Terminal.tsx,ConversationList.tsx}`, `src/lib/ws.ts`

- [ ] `lib/ws.ts`：开 WS 到 stream 端点，收 `data`→`term.write`，`term.onData`→发 `input`，resize→发 `resize`。断线自动重连。
- [ ] `Terminal.tsx`：xterm + fit-addon；底部功能键栏（Esc=`\x1b`、Ctrl+O=`\x0f`、Ctrl+C=`\x03`、↑=`\x1b[A`、↓=`\x1b[B`、Tab=`\t`、Enter=`\r`）。
- [ ] `ConversationList`：列会话+存活态，新建/进入/关闭。
- [ ] 手动验证：开会话→看到 claude TUI→发提示词→流式输出→关页面重开→恢复。
- [ ] Commit：`feat(web): terminal passthrough`。

### Task 12: 里程碑 1 集成 + 部署骨架

**Files:** Create `deploy/README.md`, `deploy/docker-compose.cloudflared.yml`(示例), `README.md`, Modify `plugins/staticSite.ts`

- [ ] `staticSite`：生产托管 `apps/web/dist`。
- [ ] `README`：本机起服务（宿主进程）、Cloudflare connector 指向、设口令的步骤。
- [ ] `npm run build` 全绿；端到端冒烟（登录→加 sample-research→开会话→流式）。
- [ ] Commit：`feat: milestone 1 integration`。

---

## 里程碑 2：文件浏览 + 多会话

### Task 13: 只读文件浏览（后端）

**Files:** Create `apps/server/src/lib/files.ts`, `routes/files.ts`, Test `files.test.ts`

- [ ] `listDir(project, relPath)`、`readFile(project, relPath)`：先 `realpath` 再断言 `startsWith(project.path)`（结合 `browseRoots`），拒绝 `../`/符号链接逃逸；文件返回 mime + 文本/二进制判定；大文件截断。
- [ ] 路由 `GET .../files?path=`、`GET .../file?path=`（requireAdmin）。
- [ ] 测试：列目录；越界 `../etc/passwd` 拒绝；符号链接逃逸拒绝；二进制识别。
- [ ] Commit：`feat(server): readonly file browser`。

### Task 14: 文件浏览（前端）

**Files:** Create `apps/web/src/components/FileBrowser.tsx`

- [ ] 列表式逐层进、面包屑返回；文本高亮、图片内联、二进制提示下载。
- [ ] Commit：`feat(web): file browser`。

---

## 里程碑 3：task/evidence

### Task 15: 解析 + 写回（后端）

**Files:** Create `apps/server/src/lib/taskEvidence.ts`, `routes/taskEvidence.ts`, Test `taskEvidence.test.ts`(+fixtures)

- [ ] 解析 `docs/tasks/INDEX.md`、`docs/evidence/INDEX.md` 表格行 + 各 `NNN-*.md`：编号、标题、优先级、状态（待办/进行/完成/废弃，依据删除线/箭头标记）、task↔evidence 链接。
- [ ] 写回：改状态/链接/标签只动 INDEX 表格行或 front-matter；原子写 + `.bak`；幂等。
- [ ] 路由：`GET .../tasks`、`GET .../evidence`、`PATCH .../tasks/:num {status?,links?,tags?}`。
- [ ] 测试：fixture 解析正确；写回后重解析一致；备份生成；正文不变。
- [ ] Commit：`feat(server): task/evidence`。

### Task 16: task/evidence 面板（前端）

**Files:** Create `apps/web/src/components/TaskEvidenceBoard.tsx`

- [ ] 渲染 markdown（含图片）；列表/看板显示编号/优先级/状态；task↔evidence 互跳；改状态/建链接/打标签 → PATCH。
- [ ] Commit：`feat(web): task/evidence board`。

### Task 17: 收尾

- [ ] 全量 `npm test`、`npm run build` 绿；更新 README；端到端走查三个里程碑。
- [ ] Commit：`docs: finalize remote-cc`。

---

## 自检

- **Spec 覆盖**：§5 鉴权→T4；§6 注册表→T5；§7 会话引擎→T6/7/8/11；§8 文件→T13/14；§9 task/evidence→T15/16；§11 部署→T12；§13 设计→T9；§14 测试→各任务共置。无遗漏。
- **占位符**：无 TBD；关键模块给了接口与控制序列。
- **类型一致**：`sessionName`/`newOrAttachArgs`/`createPtyBridge`/`SessionRegistry` 跨任务命名一致；WS 帧类型与 shared/ws.ts 一致。
